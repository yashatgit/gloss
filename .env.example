# Copy to .env and fill in the providers you want to use.
# Each provider's models are only selectable in the app if its key is present.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# PORT=8787
# GLOSS_DATA_DIR=/custom/path
